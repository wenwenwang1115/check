services:
  - type: web
    name: check-system
    env: node
    plan: starter
    buildCommand: npm install && npm run build && npm run build:api
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
